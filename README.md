My first training for creating a Spting Boot application with Thymeleaf and SQL Server.

You can add tasks and remove added tasks.

Data base containes a table with id(int), title(nvarchar), completed(bit), created_at(datetime2), updated_at(datetime2).
